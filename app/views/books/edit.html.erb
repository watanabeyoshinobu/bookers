<h1>Editing Book</h1>
 <%= form_for(@book) do |f| %>
    <h4>title</h4>
    <%= f.text_field :title %>

    <h4>body</h4>
    <%= f.text_area :body %>

    <%= f.submit 'Update Book' %>

<% end %>

   <%= link_to 'Show', book_path(@book) %>

   <%= link_to 'Back', books_path %>